
@using WebApplication.Models;
@model client_accounts

@{

    <!-- Sign up -->
    <div class="modal modal-logup">
        <div class="modal__overlay"></div>

        <div class="modal__body">
            <!--register form-->
            <div class="auth-form">
                <div class="auth-form__container" action="/Client/DangKy" method="post">
                    <div class="auth-form__header">
                        <h3 class="auth-form__heading">ĐĂNG KÝ</h3>
                    </div>
                    <div class="modals-close">
                        <i class="ti-close"></i>
                    </div>

                    <div class="auth-form__form">
                        <div class="auth-form__group">
                            <label class="auth-form__label">Tên tài khoản</label>
                            <input type="text" name="matk" class="auth-form__input" placeholder="Nhập tên tài khoản">
                        </div>
                        <div class="auth-form__group">
                            <label class="auth-form__label">Email</label>
                            <input type="text" name="email" class="auth-form__input" placeholder="Nhập Email">
                        </div>
                        <div class="auth-form__group">
                            <label class="auth-form__label">Mật khẩu</label>
                            <input type="text" name="matkhau" class="auth-form__input" placeholder="Nhập mật khẩu">
                        </div>
                        <div class="auth-form__group">
                            <label class="auth-form__label">Xác nhận mật khẩu</label>
                            <input type="text" name="matkhau" class="auth-form__input" placeholder="Nhập lại mật khẩu">
                        </div>
                    </div>

                    <div class="auth-form__controls">
                        <button class="btn btn--primary btn__signin">Đăng ký</button>
                    </div>

                    <div class="auth-form__socials">
                        <a href="" class="auth-form__socials--fb btn btn--with-icon">
                            <i class="auth-form__socials-icon fab fa-facebook-square"></i>
                            <span class="auth-form__socials-title">
                                Kết nối với Facebook
                            </span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="~/js/signInSignUp.js"></script>

}